# Yamatana AI IME v0.1.4-beta

## 修正

- Spaceキーによる明示変換で、AIが正しい候補を返してもMozc側の固定500msタイムアウトにより結果が破棄され、元のMozc候補順へ戻る問題を修正しました。
- 通常入力中は従来どおりAI推論を行わず、明示変換時だけ最大2秒の応答時間を確保します。
- CPU推論の対象を上位8候補に絞り、同じ候補数で事前ウォームアップすることで変換待ち時間を抑えました。
- CPU版ランタイムしか同梱されておらず「自動選択」でもGPUが使われなかった問題を修正し、DirectML対応GPUを実際に利用できるようにしました。
- `主戦`＋`率`のようにMozcが誤った位置で分節し、意図した単語が各セグメントの候補に存在しない場合、短い複合語を一度結合してMozc辞書候補を作り直してからAIで選ぶようにしました。
- 名前付きパイプの再生成中に接続するとAI変換が即座に失敗する競合を修正しました。
- 「この曲の主旋律は」のように文脈で判別できる同音語について、AIの並べ替え結果が実際の変換候補へ反映されるようになりました。
