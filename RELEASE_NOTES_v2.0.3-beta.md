# Yamatana AI IME v2.0.3-beta

v2.0.2-beta のUI刷新を引き継ぎつつ、AI候補順位の決定方法を見直したBetaです。

## 変換順位の改善

- Mozcの元順位に対して `-0.1 × 元順位` を加える線形ペナルティを廃止しました。
- AIがMozcの1位候補を上回った**実際のスコア差**を見て、1位を入れ替えるか判断します。
- 元の候補が2位でも20位でも、必要なAIスコア差は同じです。下位候補だからという理由だけで昇格しにくくなることはありません。
- 文脈が短い場合だけMozc 1位を少し保守的に守り、文脈が十分ある場合はAIの判断を優先します。
- 手書きの文脈ボーナスを `+100` 級から `+0.40` の補助値へ縮小し、ルールがAIスコアを上書きしないようにしました。
- 辞書外候補への固定ペナルティも `1.5` から `0.40` に縮小しました。

## GPU / 推論

- 配布版ONNXバックエンドは引き続き `onnxruntime-directml` を使用します。
- 自動選択またはGPU設定では `DmlExecutionProvider` をCPUより先に指定します。
- ONNX Session作成後に実際に有効になったProviderを確認し、GPU固定設定なのにDirectMLが有効化されなかった場合は明示的にエラーにします。

## 注意

**Unsigned Beta:** このMSIはコード署名前のBetaビルドです。Windowsの警告が表示される場合があります。
