# Yamatana AI IME v2.0.0-beta

## 主な変更

- IME向けに蒸留した70.1M parameterのRuri v3 studentモデルへ切り替えました。310M版と比較してGPU FP16モデルは約631MBから約141MB、CPU INT8モデルは約318MBから約71MBになります。
- Mozcが返した全候補を省略せず、`context + candidate_i` の配列として単一バッチへまとめ、ONNX Runtimeの `session.run()` 1回で採点します。従来の上位8候補制限を撤廃しました。
- `ひこうしょうねん` がMozcの連語処理で `飛行少年` の1分節に潰れ、`非行` が候補から消える問題を修正しました。
- 短い複合入力では意味のある既存境界を先に保護し、後続語を文脈として各分節の候補を順位付けします。1分節に潰れた場合も、内蔵辞書で両側が語として成立する境界が一意なら復元します。
- `主戦`＋`率` のような短い接尾要素は結合して `主旋律` などの全語候補を生成するため、結合が必要なケースと分割が必要なケースを使い分けます。
- 通常入力中はAI推論を行わず、0.5秒以上の保留またはSpace/変換キーによる明示変換時だけ推論する既存の入力保護を維持します。
- DirectML対応GPUを自動選択し、利用できない場合はCPU INT8へフォールバックします。
