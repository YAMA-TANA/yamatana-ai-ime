# Yamatana AI IME (MOZC Ver) v0.1.1-beta

## 修正

- MSIの製品バージョンとAIランタイムEXEのファイルバージョンを同期しました。更新インストール時にWindows Installerが古いランタイムを残し、新しいMozcとのプロトコル不一致でAI変換が失敗する問題を修正しています。
- 変換のたびにAIを実行し、前後の文脈を使って全FREEセグメントを順位付けする現行ランタイムを同梱しました。
- 配布検証でMSIとAIランタイムのバージョン不一致を検出する回帰チェックを追加しました。

MSIの内部ProductVersionは、既存の開発版 `1.0.3.0` から確実に更新できる `1.0.4.0` です。公開リリース名は `v0.1.1-beta` です。

## 更新時の注意

`v0.1.0-beta` からはこのMSIをそのまま実行して更新できます。インストール完了後はサインアウトまたは再起動し、Yamatana AI IMEを選び直してください。

このBetaのMSI、DLL、EXEは未署名です。公開ハッシュを照合し、検証環境で使用してください。
