# Yamatana AI IME v2.0.6-beta

## LoRA3 + LoRA6 アンサンブル

- 70M候補再ランキングをLoRA3（25%）とLoRA6 preceding-only（75%）のアンサンブルへ更新。
- 3文字以下の短い読みでは、両モデルを50%ずつにする短読み補正を適用。
- CPU用INT8とDirectML用FP16の両方を配布し、実行時の演算方式に合わせて2モデルを選択。
- 単一モデル用の保守的な安全ゲートは、アンサンブル時の較正済み設定へ切り替え。

## 検証

`build/runtime_ensemble_lora3_lora6_final.json` の新規評価結果：

| セット | 正解率 |
| --- | ---: |
| strict120 | 120/120 (100.00%) |
| fresh100 | 97/100 (97.00%) |
| preceding152 | 135/152 (88.82%) |

前文脈を使う実運用セットでは、助詞を含む変換範囲、途中の変換範囲、候補なしを含めて評価しています。
