# Contributing

Issue、再現手順、ドキュメント修正、テスト、コード変更を歓迎します。本プロジェクトは日本語での報告を受け付けます。

## 開発手順

1. リポジトリをforkし、目的ごとに短いブランチを作成します。
2. Python 3.12 x64で `python -m pip install -r requirements-dev.txt` を実行します。
3. `python -m pytest -q` を通します。
4. モデルが必要な試験では `powershell -File scripts/fetch-model.ps1` を実行します。固定SHA-256が一致しないファイルは使用しないでください。
5. Pull Requestには目的、利用者への影響、試験結果、ライセンス上の追加事項を記載します。

## 変更時の注意

- AI初期OFF、ローカル処理、Mozcフォールバック、アンインストール可能性を維持してください。
- 新しいネットワーク通信、テレメトリ、常駐処理、管理者権限、外部データを追加する変更は、PRで明示し `PRIVACY.md` と第三者ライセンスを更新してください。
- 大容量モデル、学習データ、ビルド成果物、個人パス、キャッシュ、秘密情報をcommitしないでください。
- 外部コードやデータを追加するときは、出典、固定revision、ライセンス、配布可否を `THIRD_PARTY_LICENSES.md` へ追記してください。
- 署名工程、ワークフロー、インストーラー、依存取得スクリプトはセキュリティ上重要です。レビューなしでReleaseへ使用しません。

提出されたContributionは、別途明示しない限りApache License 2.0の条件で提供されるものとします。
